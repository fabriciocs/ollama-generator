{
  "name": "ollama-generator",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "configure": "npm run configure --prefix backend && npm run configure --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "verify": "npm run verify --prefix backend && npm run verify --prefix frontend",
    "start:windows": "powershell -NoProfile -ExecutionPolicy Bypass -File ./start-dev-windows.ps1"
  }
}
